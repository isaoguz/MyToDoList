import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'full-capacite-services',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
